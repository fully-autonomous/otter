{
  "name": "@noms/tui",
  "version": "0.1.0",
  "description": "Noms Terminal UI built with OpenTUI",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "echo 'Build complete'",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
